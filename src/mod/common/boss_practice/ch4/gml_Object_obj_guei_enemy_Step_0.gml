/// PATCH

/// AFTER
    if (scr_isphase("bullets") && attacked == 0)
    {
        rtimer += 1;
/// CODE
if (is_double_guei() && global.bossPractice)
{
    myattackchoice = practice_patterns[global.bossTurn][myself];
}
/// END